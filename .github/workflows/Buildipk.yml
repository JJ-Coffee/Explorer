name: Build OpenWrt IPK

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Install Dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y build-essential libncurses5-dev zlib1g-dev libssl-dev wget git

    - name: Download OpenWrt Source Code
      run: |
        git clone --depth 1 --branch master https://github.com/openwrt/openwrt.git

    - name: Configure OpenWrt
      run: |
        cd openwrt
        ./scripts/feeds update -a
        ./scripts/feeds install -a
        make defconfig

    - name: Build OpenWrt Package
      run: |
        cd openwrt
        make package/dnsmasq/compile V=s

    - name: Upload IPK
      uses: actions/upload-artifact@v2
      with:
        name: dnsmasq-12
        path: openwrt/bin/packages/armvirt_64/packages/dnsmasq_*.ipk
